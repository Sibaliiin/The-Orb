We define divisibility on the whole numbers.
Let $a,b \in \mathbb{Z}$. $a$ is divisible by $b$ if there exists an $m \in \mathbb{Z}$ where
$$
a = b\cdot m.
$$
We can also write it as $b | a$ ($b$ is a divisor of $a$).

